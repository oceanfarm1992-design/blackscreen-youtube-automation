# Python dependencies for the "Meditated Sleeping" production pipeline.
# ffmpeg is a separate system package (installed via apt in the workflow), not a pip dep.

# Audio synthesis
numpy>=1.26
soundfile>=0.12

# AI music generation (MusicGen via Replicate)
replicate>=1.0

# YouTube Data API v3 upload (queue-as-private)
google-auth>=2.28
google-auth-oauthlib>=1.2
google-api-python-client>=2.120
